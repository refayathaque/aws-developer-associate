import { combineReducers } from 'redux';
import { shortTopicSelected } from './reducer_short_topic_selected';

const rootReducer = combineReducers({
    shortTopicSelected
})

export default rootReducer;
